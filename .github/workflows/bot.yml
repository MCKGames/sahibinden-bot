name: Ticari Oyun Ürünleri İlan Botu

on:
  schedule:
    - cron: '*/10 * * * *'  # Her 10 dakikada bir çalışır (istediğin gibi */5 yap)
  workflow_dispatch:  # Manuel çalıştırma için

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Bağımlılıkları yükle
        run: pip install -r requirements.txt

      - name: Botu çalıştır
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python main.py

      - name: Seen dosyayı kaydet
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add seen_ads.json || echo "Dosya yok veya değişiklik yok"
          git commit -m "Yeni ilanlar kaydedildi" || echo "Commit edecek bir şey yok"
          git push || echo "Push edecek bir şey yok"
